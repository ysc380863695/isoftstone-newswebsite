# B1 任务计划

1. 创建 package.json（Fastify, better-sqlite3, dotenv 等）
2. 创建 .env.example
3. 创建 server/config.js（配置管理）
4. 创建 server/db/index.js（SQLite连接）
5. 创建 server/index.js（Fastify入口 + 健康检查路由）
6. 创建目录结构占位
7. 安装依赖并验证服务器启动
